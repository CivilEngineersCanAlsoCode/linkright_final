# <!-- Powered by Linkright™ -->
# Agent customization for squick-sm
# Override global defaults here per project context.

persona_overrides:
  tone: "standard"
  strictness: "high"

system_overrides:
  model: "default"
  max_output_tokens: 4000
