# Step 05: Finalize & Register

**Workflow**: Automated Test Generator
**Agent**: QA Engineer

---

## 🎯 Goal

Register the new tests into the module registry and finalize the documentation.

---

## 📋 Instructions

1.  **Update Registry**: Add entries for the new tests to the target's `module.yaml` under `metadata.tests`.
2.  **Generate Coverage Summary**: Use the results from Step 04 to populate a coverage summary.
3.  **Cleanup**: Delete any temporary sandbox files used during verification.
4.  **Finalize Report**: Move the `test-plan-{timestamp}.md` to the finalized documentation section.
5.  **Handoff**: Notify the Publicist/Linker that the module now has verified test coverage.

---

## 🚦 Next Step

Workflow Complete. The module is now QA-Ready.
