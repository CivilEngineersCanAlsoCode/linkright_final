# ChromaDB MCP Gateway Configuration
# Copy this file to ~/.autonomous-dev/chromadb/.env and fill in values
#
# Generate token with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"

MCP_AUTH_TOKEN=REPLACE_WITH_GENERATED_TOKEN
CHROMA_DATA_PATH=./data
PORT=8080
